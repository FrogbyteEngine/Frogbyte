[package]
name = "frogbyte_ecs"

# Inherit shared metadata from workspace.package.
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[lints]
# Inherit workspace.lints.
workspace = true

[dependencies]
criterion = "0.8.2"