name: AI Quality

# SECURITY: pull_request_target is intentional because this workflow requires
# secrets. The job checks out only the trusted base SHA and never executes or
# checks out pull-request-controlled code in the privileged workspace.
on:
  pull_request_target: # zizmor: ignore[dangerous-triggers] trusted base checkout only; PR code is never executed here
    types:
      - labeled

permissions:
  contents: read
  pull-requests: write
  issues: write

# Only one AI quality run may modify a PR branch at a time.
# queue: max keeps additional label-triggered runs pending instead of replacing
# them, so agent:tests, agent:docs and agent:benchmarks may be applied together.
concurrency:
  group: ai-quality-${{ github.event.pull_request.number }}
  queue: max
  cancel-in-progress: false

jobs:
  quality:
    name: ${{ github.event.label.name }}

    # The privileged workflow is intentionally limited to same-repository PRs.
    # Claude Code Action additionally checks that the triggering actor has write
    # access to the repository.
    if: >-
      github.repository == 'FrogbyteEngine/Frogbyte' &&
      github.event.pull_request.head.repo.full_name == github.repository &&
      (
        github.event.label.name == 'agent:tests' ||
        github.event.label.name == 'agent:docs' ||
        github.event.label.name == 'agent:benchmarks'
      )

    runs-on: ubuntu-24.04
    timeout-minutes: 30

    env:
      QUALITY_TASK: ${{ github.event.label.name }}

    steps:
      - name: Check command label is still active
        id: command
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          set -euo pipefail

          if gh api \
            "repos/${GITHUB_REPOSITORY}/issues/${PR_NUMBER}/labels" \
            --paginate \
            --jq '.[].name' |
            grep -Fxq "$QUALITY_TASK"; then
            echo "active=true" >> "$GITHUB_OUTPUT"
          else
            echo "active=false" >> "$GITHUB_OUTPUT"
          fi

      # pull_request_target is privileged. github.sha is the current trusted
      # default-branch commit for this event. Never check out or execute the PR
      # head in the privileged workspace.
      - name: Check out trusted base branch
        if: steps.command.outputs.active == 'true'
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.sha }}
          fetch-depth: 1
          persist-credentials: false

      - name: Capture pull request head before Claude
        id: before
        if: steps.command.outputs.active == 'true'
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          set -euo pipefail

          sha="$(
            gh api \
              "repos/${GITHUB_REPOSITORY}/pulls/${PR_NUMBER}" \
              --jq '.head.sha'
          )"

          test -n "$sha"
          echo "sha=$sha" >> "$GITHUB_OUTPUT"

      # Use a Frogbyte-owned GitHub App instead of Anthropic's built-in
      # GitHub App OIDC exchange. The token is scoped to this repository and
      # revoked automatically by actions/create-github-app-token after the job.
      - name: Create AI GitHub App token
        id: ai_app_token
        if: steps.command.outputs.active == 'true'
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          client-id: ${{ vars.FROGBYTE_AI_APP_CLIENT_ID }}
          private-key: ${{ secrets.FROGBYTE_AI_APP_PRIVATE_KEY }}
          permission-actions: read
          permission-contents: write
          permission-issues: write
          permission-pull-requests: write

      # GitHub App installation tokens cannot use the authenticated /user
      # endpoint. Resolve the bot account explicitly and pass it to Claude.
      - name: Resolve AI GitHub App identity
        id: ai_app
        if: steps.ai_app_token.outcome == 'success'
        shell: bash
        env:
          GH_TOKEN: ${{ steps.ai_app_token.outputs.token }}
          APP_SLUG: ${{ steps.ai_app_token.outputs.app-slug }}
        run: |
          set -euo pipefail

          test -n "$APP_SLUG"

          bot_login="${APP_SLUG}[bot]"
          bot_id="$(
            gh api "/users/${bot_login}" --jq '.id'
          )"

          test -n "$bot_id"

          echo "login=${bot_login}" >> "$GITHUB_OUTPUT"
          echo "id=${bot_id}" >> "$GITHUB_OUTPUT"

      - name: Run Claude quality agent
        id: claude
        if: >-
          steps.command.outputs.active == 'true' &&
          steps.ai_app_token.outcome == 'success' &&
          steps.ai_app.outcome == 'success'
        continue-on-error: true
        uses: anthropics/claude-code-action@6b082c41935b4c8a3b8b0ef85ba4ba4d9eeb8975 # v1.0.189
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          github_token: ${{ steps.ai_app_token.outputs.token }}
          bot_id: ${{ steps.ai_app.outputs.id }}
          bot_name: ${{ steps.ai_app.outputs.login }}

          additional_permissions: |
            actions: read

          prompt: |
            You are the Frogbyte quality-generation agent.

            Repository: ${{ github.repository }}
            Pull request: #${{ github.event.pull_request.number }}
            Triggering label: ${{ github.event.label.name }}

            Read the trusted base-branch CLAUDE.md before doing anything and
            obey it strictly.

            Read the pull request description, linked issue, changed files,
            relevant implementation, existing tests, documentation, and
            benchmarks using the allowed GitHub read-only commands.

            IMPORTANT SECURITY BOUNDARY:

            This workflow contains authentication credentials.

            Never execute pull-request-controlled project code in this job.
            Do not run Cargo, build scripts, tests, benchmarks, formatters,
            linters, documentation builds, project scripts, binaries, proc
            macros, or any other command derived from pull-request contents.

            Existing GitHub CI is responsible for deterministic validation
            after your changes are pushed. The trusted checkout deliberately
            does not persist the workflow GITHUB_TOKEN; use only the short-lived
            Frogbyte GitHub App credentials supplied to this action for branch
            writes.

            Perform exactly ONE task:

            - `agent:tests`:
              Generate or improve meaningful integration tests for behavior
              introduced or modified by this pull request.
              You may modify ONLY `crates/*/tests/**`.
              If useful testing requires editing production source or adding a
              dependency, make no change and explain why.

            - `agent:docs`:
              Generate or improve meaningful Rustdoc and documentation relevant
              to this pull request.
              You may modify ONLY:
              - `docs/**`;
              - `crates/*/README.md`;
              - Rust source under `crates/*/src/**/*.rs`, and only for line
                comments: `//`, `///`, or `//!`.
              Do not use block comments or `#[doc = ...]` attributes in Rust
              source during an automated documentation task.
              Executable behavior must not change.

            - `agent:benchmarks`:
              Generate or improve realistic benchmarks relevant to this pull
              request.
              You may modify ONLY `crates/*/benches/**`.
              If no suitable benchmark harness already exists, make no code
              changes and report that the task was skipped.

            Never approve, submit a formal pull request review, merge, enable
            auto-merge, mark ready for review, or alter the merge state of this
            pull request.

            Never modify `.github/**`, `AGENTS.md`, `CLAUDE.md`, Cargo
            manifests, `Cargo.lock`, or `rust-toolchain.toml`.

            Do not add dependencies.
            Do not introduce unsafe code.
            Do not weaken tests, assertions, lint rules, safety checks, or
            validation.
            Do not make unrelated production changes.

            Before finishing, inspect your complete diff and verify that every
            changed path belongs to the triggering task.

            If useful changes were made, create at most one focused commit and
            push it only to the current pull request branch.

            After completing the task, post exactly one top-level pull request
            comment using `gh pr comment`.

            The report must contain:

            - the triggering command;
            - files added or modified, or `none`;
            - local validation: `not run in privileged AI job`;
            - CI validation: `pending after push`, or `not applicable` when no
              files were changed;
            - anything intentionally skipped and why.

            If no useful change was justified, report that clearly instead.

            Do not submit a formal pull request review. Do not approve the pull
            request.

          claude_args: |
            --max-turns 20
            --allowedTools "Read,Glob,Grep,Edit,Write,Bash(git status:*),Bash(git diff:*),Bash(git log:*),Bash(gh pr view:*),Bash(gh pr diff:*),Bash(gh issue view:*),Bash(gh pr comment:*)"
            --disallowedTools "Bash(cargo:*),Bash(git commit --no-verify:*),Bash(git push --no-verify:*),Bash(git push --force:*),Bash(git push --force-with-lease:*),Bash(git rebase:*),Bash(git reset:*),Bash(gh pr review:*),Bash(gh pr merge:*)"

      # Claude Code Action can push directly to an open PR branch. Audit only
      # commits attributed to the Frogbyte GitHub App bot, so unrelated
      # maintainer commits made while this job is running are not misclassified
      # as agent changes.
      #
      # This audit uses GitHub API metadata only. It never checks out or executes
      # pull-request-controlled code.
      - name: Audit generated changes
        id: scope
        if: >-
          always() &&
          steps.command.outputs.active == 'true' &&
          steps.before.outputs.sha != '' &&
          steps.ai_app_token.outcome == 'success' &&
          steps.ai_app.outcome == 'success'
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          BEFORE_SHA: ${{ steps.before.outputs.sha }}
          TASK_LABEL: ${{ github.event.label.name }}
          AI_BOT_LOGIN: ${{ steps.ai_app.outputs.login }}
        run: |
          set -euo pipefail

          echo "valid=true" >> "$GITHUB_OUTPUT"
          echo "pushed=false" >> "$GITHUB_OUTPUT"

          after_sha="$(
            gh api \
              "repos/${GITHUB_REPOSITORY}/pulls/${PR_NUMBER}" \
              --jq '.head.sha'
          )"

          test -n "$after_sha"

          if [[ "$after_sha" == "$BEFORE_SHA" ]]; then
            echo "No commit was pushed while the AI task was running."
            exit 0
          fi

          compare_json="$RUNNER_TEMP/ai-quality-compare.json"

          gh api \
            "repos/${GITHUB_REPOSITORY}/compare/${BEFORE_SHA}...${after_sha}" \
            > "$compare_json"

          mapfile -t claude_commits < <(
            python3 - "$compare_json" "$AI_BOT_LOGIN" <<'PY'
          import json
          import sys

          compare_path, bot_login = sys.argv[1], sys.argv[2]

          with open(compare_path, encoding="utf-8") as handle:
              data = json.load(handle)

          for commit in data.get("commits", []):
              author = (commit.get("author") or {}).get("login")
              committer = (commit.get("committer") or {}).get("login")
              if author == bot_login or committer == bot_login:
                  print(commit["sha"])
          PY
          )

          if [[ "${#claude_commits[@]}" -eq 0 ]]; then
            echo "No ${AI_BOT_LOGIN} commit was found in the branch changes."
            exit 0
          fi

          if [[ "${#claude_commits[@]}" -gt 1 ]]; then
            echo "pushed=true" >> "$GITHUB_OUTPUT"
            echo "::error::The AI task created more than one Claude commit."
            echo "valid=false" >> "$GITHUB_OUTPUT"
            exit 1
          fi

          echo "pushed=true" >> "$GITHUB_OUTPUT"
          echo "commit_sha=${claude_commits[0]}" >> "$GITHUB_OUTPUT"

          fail=0

          for commit_sha in "${claude_commits[@]}"; do
            commit_pages="$RUNNER_TEMP/ai-quality-${commit_sha}-pages.json"

            # The Get-a-commit endpoint paginates the changed-file list.
            # Slurp every response page into one JSON array before auditing.
            gh api \
              --method GET \
              --paginate \
              --slurp \
              -f per_page=100 \
              "repos/${GITHUB_REPOSITORY}/commits/${commit_sha}" \
              > "$commit_pages"

            if ! python3 - "$commit_pages" "$TASK_LABEL" <<'PY'
          import json
          import re
          import sys

          commit_path, task = sys.argv[1], sys.argv[2]

          with open(commit_path, encoding="utf-8") as handle:
              pages = json.load(handle)

          files = [
              changed_file
              for page in pages
              for changed_file in page.get("files", [])
          ]

          # GitHub documents a maximum of 3,000 changed files for this
          # paginated endpoint. Fail closed at the limit instead of claiming
          # that the audit is complete if the API may have truncated the diff.
          if len(files) >= 3000:
              print(
                  "::error::Claude commit reached GitHub's 3,000-file audit "
                  "limit; refusing to treat the scope audit as complete."
              )
              raise SystemExit(1)

          test_path = re.compile(r"^crates/[^/]+/tests/")
          docs_path = re.compile(r"^docs/")
          readme_path = re.compile(r"^crates/[^/]+/README\.md$")
          rust_source_path = re.compile(r"^crates/[^/]+/src/.*\.rs$")
          bench_path = re.compile(r"^crates/[^/]+/benches/")

          def fail(message: str) -> None:
              print(f"::error::{message}")
              raise SystemExit(1)

          def rust_patch_is_comment_only(patch: str | None) -> bool:
              # Fail closed if GitHub did not provide a textual patch.
              if patch is None:
                  return False

              for line in patch.splitlines():
                  if line.startswith(("+++", "---", "@@")):
                      continue

                  if not line.startswith(("+", "-")):
                      continue

                  changed = line[1:].lstrip()

                  # Blank-line-only changes are harmless for documentation.
                  if not changed:
                      continue

                  # Keep the mechanical contract intentionally simple:
                  # Rust source edits made by agent:docs may only add/remove
                  # line comments. This includes //, /// and //!.
                  if not changed.startswith("//"):
                      return False

              return True

          for changed_file in files:
              filename = changed_file["filename"]

              if task == "agent:tests":
                  if not test_path.match(filename):
                      fail(
                          "agent:tests changed an out-of-scope file: "
                          f"{filename}"
                      )

              elif task == "agent:benchmarks":
                  if not bench_path.match(filename):
                      fail(
                          "agent:benchmarks changed an out-of-scope file: "
                          f"{filename}"
                      )

              elif task == "agent:docs":
                  if docs_path.match(filename) or readme_path.match(filename):
                      continue

                  if rust_source_path.match(filename):
                      if not rust_patch_is_comment_only(
                          changed_file.get("patch")
                      ):
                          fail(
                              "agent:docs changed executable or non-comment "
                              f"Rust content in: {filename}"
                          )
                      continue

                  fail(
                      "agent:docs changed an out-of-scope file: "
                      f"{filename}"
                  )

              else:
                  fail(f"Unknown AI quality task: {task}")

          print("Claude-generated changes satisfy the task scope.")
          PY
            then
              fail=1
            fi
          done

          if [[ "$fail" -ne 0 ]]; then
            echo "valid=false" >> "$GITHUB_OUTPUT"
            exit 1
          fi

      - name: Detect Claude subscription limit
        id: claude_limit
        if: >-
          always() &&
          steps.command.outputs.active == 'true' &&
          steps.ai_app_token.outcome == 'success' &&
          steps.ai_app.outcome == 'success'
        shell: bash
        env:
          CLAUDE_STEP_OUTCOME: ${{ steps.claude.outcome }}
          CLAUDE_CONCLUSION: ${{ steps.claude.outputs.conclusion }}
          EXECUTION_FILE: ${{ steps.claude.outputs.execution_file }}
        run: |
          set -euo pipefail

          echo "reached=false" >> "$GITHUB_OUTPUT"
          echo "failed=false" >> "$GITHUB_OUTPUT"

          if [[ "$CLAUDE_STEP_OUTCOME" != "success" ||
                "$CLAUDE_CONCLUSION" == "failure" ]]; then
            echo "failed=true" >> "$GITHUB_OUTPUT"
          else
            exit 0
          fi

          # Only match messages that specifically describe included usage
          # exhaustion. Generic rate-limit errors may be transient throttling
          # and must not automatically consume the Codex fallback.
          if [[ -z "$EXECUTION_FILE" || ! -f "$EXECUTION_FILE" ]]; then
            exit 0
          fi

          if grep -Eqi \
            'usage limit|limit reached|5-hour limit|weekly limit|exceed.*usage' \
            "$EXECUTION_FILE"; then
            echo "reached=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Report AI GitHub authentication failure
        if: >-
          always() &&
          steps.command.outputs.active == 'true' &&
          (
            steps.ai_app_token.outcome == 'failure' ||
            steps.ai_app.outcome == 'failure'
          )
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          TASK_LABEL: ${{ github.event.label.name }}
        run: |
          set -euo pipefail

          gh api \
            --method POST \
            "repos/${GITHUB_REPOSITORY}/issues/${PR_NUMBER}/comments" \
            -f body="AI Quality infrastructure authentication failed before Claude could process \`${TASK_LABEL}\`. The Frogbyte GitHub App token or bot identity could not be created. Claude quota fallback was intentionally not triggered. Check the AI Quality workflow run and the GitHub App installation, Client ID, private key, and permissions."

      # If Claude already pushed a scoped commit, do not start another writer.
      # A late quota failure (for example while posting the final report) must
      # not cause Codex to duplicate or conflict with completed Claude output.
      - name: Report Claude push completed before quota failure
        if: >-
          always() &&
          steps.claude_limit.outputs.reached == 'true' &&
          steps.scope.outputs.pushed == 'true' &&
          steps.scope.outputs.valid == 'true'
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          TASK_LABEL: ${{ github.event.label.name }}
          COMMIT_SHA: ${{ steps.scope.outputs.commit_sha }}
        run: |
          set -euo pipefail

          gh api \
            --method POST \
            "repos/${GITHUB_REPOSITORY}/issues/${PR_NUMBER}/comments" \
            -f body="Claude reached its included usage limit after already pushing a scoped commit for \`${TASK_LABEL}\` (${COMMIT_SHA}). Codex fallback was intentionally skipped to avoid duplicate or conflicting work. Existing CI remains responsible for validating the pushed commit."

      # Submit the asynchronous Codex handoff before clearing queued labels.
      # If this step fails, queued labels are preserved and the current command
      # label remains visible for an explicit retry.
      - name: Trigger Codex fallback
        id: codex_fallback
        if: >-
          steps.claude_limit.outputs.reached == 'true' &&
          steps.scope.outputs.pushed != 'true'
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.CODEX_FALLBACK_PAT }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          TASK_LABEL: ${{ github.event.label.name }}
        run: |
          set -euo pipefail

          case "$TASK_LABEL" in
            agent:tests)
              TASK="Generate or improve meaningful integration tests for this pull request. Modify only crates/*/tests/**. Do not change production behavior."
              ;;
            agent:docs)
              TASK="Generate or improve meaningful Rustdoc and documentation for this pull request. Do not change executable behavior."
              ;;
            agent:benchmarks)
              TASK="Generate or improve realistic benchmarks for this pull request. Modify only crates/*/benches/**. If no benchmark harness exists, make no code changes and explain why."
              ;;
            *)
              exit 1
              ;;
          esac

          BODY="@codex FROGBYTE_QUALITY_FALLBACK

          Claude could not continue because its included usage limit appears to have been reached.

          The Codex handoff was submitted before queued agent labels are cleared.
          Do not apply another agent:* label until this Codex task has completed.

          $TASK

          Follow the explicit quality-generation fallback rules in AGENTS.md.

          Never approve, submit an approving review, merge, enable auto-merge,
          mark ready for review, or change the merge state of this PR.

          Do not modify CI, GitHub workflows, CLAUDE.md, Cargo manifests,
          Cargo.lock, or rust-toolchain.toml. Do not add dependencies.

          Run the relevant Rust validation in the Codex environment. Commit and
          push only useful quality changes to this PR branch."

          gh api \
            --method POST \
            "repos/${GITHUB_REPOSITORY}/issues/${PR_NUMBER}/comments" \
            -f body="$BODY"

          echo "submitted=true" >> "$GITHUB_OUTPUT"

      # Codex runs asynchronously after the @codex comment is accepted. Only
      # now clear queued command labels so a failed handoff cannot destroy the
      # pending task queue.
      - name: Clear queued command labels after Codex handoff
        if: >-
          always() &&
          steps.codex_fallback.outputs.submitted == 'true'
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          set -euo pipefail

          for label in agent:tests agent:docs agent:benchmarks; do
            gh api \
              --method DELETE \
              "repos/${GITHUB_REPOSITORY}/issues/${PR_NUMBER}/labels/${label}" \
              >/dev/null 2>&1 || true
          done

      - name: Report Codex fallback handoff failure
        if: >-
          always() &&
          steps.claude_limit.outputs.reached == 'true' &&
          steps.scope.outputs.pushed != 'true' &&
          steps.codex_fallback.outcome == 'failure'
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          TASK_LABEL: ${{ github.event.label.name }}
        run: |
          set -euo pipefail

          gh api \
            --method POST \
            "repos/${GITHUB_REPOSITORY}/issues/${PR_NUMBER}/comments" \
            -f body="Claude reached its included usage limit while processing \`${TASK_LABEL}\`, but the Codex fallback handoff failed. Queued agent labels were preserved and the current command label was left in place. Fix the fallback credential or transient GitHub error, then remove and re-apply the command label to retry."

      - name: Report non-quota Claude failure
        if: >-
          always() &&
          steps.command.outputs.active == 'true' &&
          steps.claude_limit.outputs.failed == 'true' &&
          steps.claude_limit.outputs.reached != 'true'
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          TASK_LABEL: ${{ github.event.label.name }}
        run: |
          gh api \
            --method POST \
            "repos/${GITHUB_REPOSITORY}/issues/${PR_NUMBER}/comments" \
            -f body="Claude execution failed while processing \`${TASK_LABEL}\` after GitHub App authentication succeeded, but the failure was not confidently identified as an included-usage quota limit. Codex fallback was intentionally not triggered. Check the AI Quality workflow run."

      - name: Report scope violation
        if: always() && steps.scope.outputs.valid == 'false'
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          TASK_LABEL: ${{ github.event.label.name }}
        run: |
          gh api \
            --method POST \
            "repos/${GITHUB_REPOSITORY}/issues/${PR_NUMBER}/comments" \
            -f body="The \`${TASK_LABEL}\` AI task pushed at least one file outside its allowed path scope. The AI Quality run failed. Do not merge those generated changes until they are reviewed and corrected."

      # A command label is reusable after the previous invocation has removed
      # it. Do not remove and re-apply the same label while its current run is
      # still active.
      - name: Remove command label
        if: >-
          always() &&
          steps.command.outputs.active == 'true' &&
          !(
            steps.claude_limit.outputs.reached == 'true' &&
            steps.scope.outputs.pushed != 'true' &&
            steps.codex_fallback.outcome == 'failure'
          )
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          TASK_LABEL: ${{ github.event.label.name }}
        run: |
          gh api \
            --method DELETE \
            "repos/${GITHUB_REPOSITORY}/issues/${PR_NUMBER}/labels/${TASK_LABEL}" \
            >/dev/null 2>&1 || true

      - name: Fail workflow when AI GitHub authentication failed
        if: >-
          always() &&
          steps.command.outputs.active == 'true' &&
          (
            steps.ai_app_token.outcome == 'failure' ||
            steps.ai_app.outcome == 'failure'
          )
        shell: bash
        run: exit 1

      - name: Fail workflow when Claude failed without fallback
        if: >-
          always() &&
          steps.command.outputs.active == 'true' &&
          steps.claude_limit.outputs.failed == 'true' &&
          steps.claude_limit.outputs.reached != 'true'
        shell: bash
        run: exit 1
